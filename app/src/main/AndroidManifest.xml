<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="roma.promo.nailson.romapromo">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <uses-feature android:name="android.hardware.location.gps" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/iconeroma"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/iconeroma"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
                   </activity>
        <activity android:name=".CheckIn" />
        <activity android:name=".Realizadas" />
        <activity android:name=".Concorrencia" />
        <activity android:name=".Acoes" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDSeeYoh3iZeEl3n3YT4AwofFtnl5CIbR4" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".CadastroUsuario" />
        <activity android:name=".LoginUsuario" />
        <activity android:name=".PerfilUsuario" />
        <activity android:name=".BrowsePictureActivity" />
        <activity android:name=".ImagemAcao" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
        <activity android:name=".CadAcoesConcorrentes" />
        <activity android:name=".RealizadasConcorrente" />
        <activity android:name=".PontosFuncionarios" />
        <activity android:name=".PontosRegistrados" />
        <activity android:name=".InicioLogin" />
        <activity android:name=".TesteArduino">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>